function valueMax(numN, numM) {
  return `O valor maior é : ${Math.max(numN, numM)}`;
}

function valueMin(numN, numM) {
  return `O valor minimo é  : ${Math.min(numN, numM)}`;
}

console.log(valueMax(20, 30));
console.log(valueMin(20, 30));
