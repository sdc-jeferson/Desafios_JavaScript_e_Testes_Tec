// 11 - Somatório 1 a N
let num = 2;

for (let x = 0; x < 10; x++) {
  console.log(`-----------Tabudada ${x}--------------`);
  for (let index = 0; index < 10; index++) {
    console.log(`${x} * ${index} = ${x * index}`);
  }
}
